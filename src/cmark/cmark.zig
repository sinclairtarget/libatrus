//! Logic specified by the common mark specification that doesn't fit neatly
//! into either lexing, parsing, or rendering.

pub const character_refs = @import("character_refs.zig");
pub const uri = @import("uri.zig");
